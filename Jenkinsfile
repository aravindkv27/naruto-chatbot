pipeline{

    agent any

    stages{
        
        stage("build"){

            steps{
                echo 'Build Application'
            }

            steps{
                pip install python-dotenv
            }
        }
    }
}